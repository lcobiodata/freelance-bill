import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#1976D2" },   // Professional blue
    secondary: { main: "#FFC107" }, // Contrasting yellow
    background: { default: "#f4f6f8" },
  },
});

export default theme;
